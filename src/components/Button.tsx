const Button = () => {
  return (
    <button className="px-10 py-3 bg-[var(--primary-color)] text-[var(--black-color)] duration-200 hover:bg-white">
      Skontaktuj się
    </button>
  );
};

export default Button;
