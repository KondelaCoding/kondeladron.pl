const Button = () => {
  return (
    <button className="px-10 py-3 bg-[var(--primary-color)] text-[var(--black-color)] duration-100 shadow-lg mb-20 hover:bg-white active:scale-95 active:opacity-80">
      Skontaktuj się
    </button>
  );
};

export default Button;
